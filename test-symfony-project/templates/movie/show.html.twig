<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div class="container mt-4">
        <h1>{{ movie.title }}</h1>
        <p class="text-muted">Год выпуска: {{ movie.year }}</p>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Описание</h5>
                <p class="card-text">{{ movie.description ?? 'Описание отсутствует' }}</p>
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_movie') }}" class="btn btn-secondary">
                ← Назад к списку фильмов
            </a>
        </div>
    </div>
</body>

</html>